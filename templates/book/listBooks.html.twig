 <h1>List of books</h1>
<table border="1">
<tr>
    {# <th> id </th> #}
    <th>title</th>
    <th> Publication date </th>
    <th> Published </th>
    <th> Category </th>   
    <th> Author </th>
    <th> Actions </th>
    
</tr>

      <tr>  {% for b in book %} </tr>
      {# <td> {{ b.id }}</td> #}
      <td>{{ b.title }}</td>
      <td> {{ b.publicationDate | date('y-m-d') }}</td>
      <td>{{ b.published }}</td>
      <td>{{ b.category }}</td>    
      <td> {{ b.author.username  }}</td>
      <td>  
      
      <a href="{{path('book_edit',{'id':b.id})}}" > edit </a>  &nbsp;
      <a href="{{path('book_delete',{'id':b.id})}}">delete</a> &nbsp;
    
      <a href="" > show </a> 
      </td>
      {# <td>
      <a href="{{ path('author_delete', {'id': a.id}) }}">delete</a>
      <a href="{{ path('author_edit', {'id': a.id}) }}" > edit </a> 
      </td> #}
        
      
    {% endfor %}    
</table>
<br>
<p style="margin-left:250px;"> Number of published books: {{published}} </p>
<p style="margin-left:250px;"> Number of unpublished books: {{unpublished}} </p>
<button style="margin-left:300px;"> <a href="{{path('add_book')}}">Create new </a> </button>
