<center> <h1>Liste des aeroports</h1> </center>
<table border="1">
<tr>
    <th> id </th>
    <th>Nom</th>
    <th>Actions</th>
</tr>

      <tr>  {% for a in aero %} </tr>
      <td> {{ a.id }}</td>
      <td>{{ a.nom }}</td>
      
      <td>
      {# <a href="{{ path('author_delete', {'id': a.id}) }}">delete</a> #}
      {# <a href="{{ path('author_edit', {'id': a.id}) }}" > edit </a> #}
      </td>
        
      
    {% endfor %}    
</table>
